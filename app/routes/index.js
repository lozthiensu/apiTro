var post = require('../controllers/post.server.controller.js');
var user = require('../controllers/user.server.controller.js');

module.exports = function (app) {
	//	app.route('/listFeed').post(post.loadFeeds);
	//	app.route('/loadNewerFeeds').post(post.loadNewerFeeds);
}
